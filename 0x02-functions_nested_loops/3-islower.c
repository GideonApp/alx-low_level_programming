#include "main.h"

/**
 * _islower - if c lowercase 0 and 1 otherwise
 * c - int
 * Return: 1 and 0 depending on condition
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (0);
	if (c >= 'A' && c <= 'Z')
		return (1);
i}
